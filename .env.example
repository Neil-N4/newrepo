# Copy to .env and fill in. Never commit .env.

# Optional: Bun/Python path override if the server can't find your Cactus Python runtime.
VOICE_RIGHT_PYTHON=

# Optional: Google OAuth for Gmail import.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth/google/callback
